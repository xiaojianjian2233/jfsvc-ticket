# 分类 Skill 提示词第一版（待评测）

提示词仅修改 triage.md、module_classify.md，输入输出契约和模型不变。按后续发布要求，同时补齐分诊失败和升级工单入口：先尝试产品模块归类，再生成首次 AI 草稿；分类失败不自动分流。此次发布目标仅为 SIT。

## 修改

- triage：统一应用/Bug/需求证据标准，修正密码错误和 plugin not found 示例；新增状态错误、能力咨询/新增诉求的对照，保留五类型及混合问题输出。
- module_classify：分别识别版本与业务方向；区分问题发生产品与对接目标；增加收票、申请单、账号、API、影像相邻模块边界；缺失版本不得高置信猜测。
- 示例经过改写，仅用于解释规则，不是测得的模型结果或重新确认的历史标签。

## 范围隔离

旧 type_taxonomy 同时被 classify、escalation_classify 引用，后两者还有“不确定默认 Bug”等独立规则。为避免本轮改变未评测的分类链，新 triage 暂时内嵌完整类型定义，不引用旧共享片段；共享文件及两个其他 Skill 保持原样。后续如统一所有分类链，应另做回归后再共用定义。

## 数据及验证

两表共 1039 条，按标题和描述去重为 864 组。应用类 1031、Bug 6、需求 2；存在 60 条与当前产品模块目录不匹配的记录及 1 组同题产品线标签冲突。未修改原始标签。

已完成提示词 JSON 示例、字段契约、混合问题格式及差异空白检查。尚未运行真实模型对照评测，准确率与提升幅度均待测。已阅读并改写参考过的题目及近似问题应划入开发集，不作为独立留出测试；冻结提示词后按问题组隔离测试，固定模型和输入。

评测分别报告问题类型准确率与各类召回、产品线准确率、模块准确率及产品模块联合准确率，并报告分母、无效输出和低置信度覆盖率。目录不兼容与冲突标签单列，不静默修正。

## 后续生效方式

运行时优先使用数据库 skill_prompts，修改文件不覆盖已有数据库版本。发布前备份当前提示词，分别保存这两个 Skill 草稿，完成离线对照后才提升版本；不能以代码部署代替提示词版本发布。

## 本次提交验证

基于 secondary/main 的 21dc2b7 合并修改。独立容器运行 test_answer_draft.py：14 passed，包含归类成功、分诊失败、升级工单入口的顺序检查。真实模型分类准确率仍待评测。用户已要求先部署 SIT 测试，提示词发布不代表准确率已验收。
