# 2026-09-15 部署源码同步

本次提交以已部署的恢复版本为准，包含线上回退版本的修复，以及后续经过 SIT 验证并发布 UAT 的修改。

- 新工单在分类前生成 AI 草稿，人工可重复触发；不因未分类或缺少标题被拦截，草稿不触发对客发送或修改处理状态。
- 图片使用正式接口顶层 images 数组；产品信息包含目录中的中文名称。
- API、附件和登录统一继承部署前缀。SIT /hub-issue/ 与 UAT /ticket-hub-uat/ 必须分别构建，不能直接复制前端产物。
- 综合/每日看板采用已确认状态及 24H/40H 超期口径，补充待处理和转研发数字卡。
- 保留线上版本的转研发重复推送保护、标签页和知识库行为，并移除旧工作目录中的硬编码开发登录凭据。

部署配置中的 AI_CS_BASE_URL 已更新为 http://123.207.158.7:5000/fpy_agent 。环境文件及密钥不进入仓库。

验证：后端相关 116 项、前端相关 53 项通过；两环境单独构建；公网页面、资源、已登录的工单列表与两看板接口正常。真实图片业务测试由用户确认成功。

2026-09-14 的 502 来自 API 请求丢失 /ticket-hub-uat 前缀，误入旧上游端口。发布顺序必须为备份、SIT 发布与验证、UAT 单独构建与发布、公网验收；仅检查后端 health 不足以完成验收。
