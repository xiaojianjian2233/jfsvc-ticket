# KSM 退回补偿需求记录（2026-09-24）

用户确认：仅增加退回补偿，退回回声判断不修改，不增加十分钟冷却。

- 正常退回找不到上一节点时，按 Ticket 当前处理人的 KSM 账号（优先 ksm_account，否则 employee_no）匹配其最新有效受理记录；无需人工选择。
- 允许同节点 ID 的受理记录，但排除协同处理、自动分单等记录；不按姓名或全局默认身份猜测。
- 必须使用实时 KSM 数据，身份缺失、候选无效或最新记录有歧义时明确失败。
- KSM 成功后记录补偿账号及目标 opercacheId，并保留原有目标节点信息供既有回声逻辑使用。
- 用户要求部署 SIT 与 UAT，并提交至唯一仓库 xiaojianjian2233/jfsvc-ticket。

验证：退回回写、KSM 入库、接管相关测试共 109 项通过；修改文件 Ruff 检查通过。
